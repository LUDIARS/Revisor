{
  "name": "review-queue-scheduling",
  "description": "Durable local review-job lane assignment and bounded execution capacity. Standard and fast work retain independent FIFO order and fixed worker partitions across the outer PR scheduler and each review-stage pool.",
  "presetRules": [],
  "templateRules": [],
  "membership": [
    {
      "pathPattern": "(^|/)src/(?:job-store|persistent-queue|review-job-scheduler|review-lane|review-stage-workers|worker-command|worker-pool|worker-state)\\.mjs$"
    },
    {
      "pathPattern": "(^|/)test/(?:job-store|review-job-scheduler|review-stage-workers|worker-command|worker-pool|worker-state)\\.test\\.mjs$"
    },
    {
      "pathPattern": "(^|/)src/local-contracts\\.mjs$"
    },
    {
      "pathPattern": "src/(?:job-store|persistent-queue|review-job-scheduler|review-lane|review-stage-workers|worker-command|worker-pool|worker-state)\\.mjs$"
    },
    {
      "namePattern": "^(validateFastLanePromotion|validateReviewRetry)$"
    },
    {
      "pathPattern": "(^|/)src/(?:queue|worker\\-entry|worker\\-spawn)\\.mjs$"
    },
    {
      "pathPattern": "(^|/)test/(?:queue|worker\\-entry|worker\\-spawn)(?:-[a-z0-9-]+)?\\.test\\.mjs$"
    },
    {
      "pathPattern": "(^|/)test/persistent-queue\\.test\\.mjs$"
    },
    {
      "pathPattern": "(^|/)test/helpers/barrier-children\\.mjs$"
    }
  ],
  "cardTemplate": "Summarise the review-queue-scheduling domain: durable lane assignment, independent FIFO order, explicit promotion, and fixed standard/fast capacity partitions.",
  "mechanics": [
    "durable-review-lane",
    "fast-lane-promotion",
    "lane-fifo-order",
    "reserved-worker-partition",
    "daemonless-queue-drain"
  ],
  "specRefs": [
    "review-fast-lane — SPEC-REVIEW-FAST-LANE-DURABILITY",
    "review-fast-lane — SPEC-REVIEW-FAST-LANE-CAPACITY",
    "review-fast-lane — SPEC-REVIEW-FAST-LANE-AUTHORITY",
    "daemonless-cli — SPEC-DAEMONLESS-PERSISTENT-QUEUE",
    "daemonless-cli — SPEC-DAEMONLESS-WORKER-DRAIN"
  ],
  "rationale": "Lane persistence and worker admission change together: the durable job record determines which fixed capacity partition may execute it. Keeping these mechanics in one domain prevents queue ordering, restart recovery, and stage-worker admission from interpreting the lane differently. PR lifecycle state, HTTP authorization, and UI rendering remain delegated to their existing domains."
}
